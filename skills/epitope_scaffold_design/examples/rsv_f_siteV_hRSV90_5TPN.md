# RSV F Site V / hRSV90 5TPN Benchmark

Use `examples/rsv_f_siteV_hRSV90_reproduction_5TPN_20260629/run_smoke_benchmark.sh`.

This example intentionally separates:

- contact-derived epitope extraction from PDB 5TPN
- RFdiffusion original example reconciliation
- backend comparison using the reconciled benchmark motif

Do not treat earlier `examples/epitope_scaffold/*5tpn*` runs as final benchmark evidence. Those are smoke-test provenance only.
