# Epitope Scaffold Handoff

- Link `run_params.json` and `job_ids.env`.
- Link motif extraction and original-case reconciliation reports.
- Link backend comparison summary and fold clustering reports.
- List selected candidates and explain why each passed.
- State which candidates are not order-ready and what expression/vector policy is missing.
- Preserve backend versions, checkpoints, commands, job IDs, runtime, thresholds, and failure notes.
