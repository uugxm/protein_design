# Weights Notes

The deployment uses symlinks for large model weights.

Current RFdiffusion weights:

- `~/protein_design/weights/RFdiffusion_models` -> `~/protein-design/apps/RFdiffusion/models`

Do not duplicate large checkpoints under `/public` unless there is a project reason.
