# Pilot Scripts

This directory is reserved for pilot-local helper notes. The executable helpers live in the repository-level `scripts/` directory so the reusable skill can call the same validation, contact-face QC, and summary code.
